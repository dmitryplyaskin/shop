type Mutation {
  createToken(createUserInput: CreateUserInput): Token
}

type Token {
  accessToken: String
  expiresIn: Int
  email: String
}

input CreateUserInput {
  email: String
  password: String
}
